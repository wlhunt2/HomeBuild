<main>
<p id="notice"><%= notice %></p>

  <h1>Builders#show</h1>

  <p>
    Builder:<br>
    <%= @builder.first_name %> <%= @builder.last_name %><br>
    <%= @builder.address %> <br>
    <%= @builder.city %>, <%= @builder.state %> <%= @builder.zip %><br>


    <%= button_to 'Destroy',
                  builder_path(@builder),
                  method: :delete,
                  data: {confirm: 'You sure?'} %>
  </p>

<%= link_to 'Edit', edit_builder_path(@builder) %> |
<%= link_to 'Back', builders_path %>
</main>